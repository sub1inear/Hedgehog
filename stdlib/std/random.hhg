import std.time

pub class Random {
    seed: u32

    pub fn Random(&mut self) {
        self.seed = time.time()
    }

    pub fn Random(&mut self, seed: u32) {
        self.seed = seed
    }

    pub fn random(&mut self) -> u32 {
        return self.seed = self.seed * 69069 + 1
    }

    pub fn random(&mut self, max: u32) -> u32 {
        return self.random() % max
    }

    pub fn random(&mut self, min: u32, max: u32) -> u32 {
        return min + self.random(max - min)
    }

    pub fn random(&mut self, min: i32, max: i32) -> i32 {
        return min + self.random(max - min)
    }
}
